# PROMPTS — 에이전트에게 보내는 메시지 모음

재무 회계팀 실습에서 에이전트에게 보내는 자연어 메시지를 순서대로 모았습니다. 유닛별로 나눠 두었고, 각 항목에 누구에게 보내는지 적어 두었습니다.
코드 블록을 통째로 복사해 대화창에 붙여넣으세요. 괄호 자리만 본인 상황에 맞게 바꿉니다.

> ⚠️ 초안 안내: 아래 메시지는 실측 전 초안입니다. 실습 검증 후 문구와 번호가 확정됩니다.

# 증권사 API 연동과 안전 경계 (8.1)

## 1. 재무 회계팀 개소 — 원칙 세우기 (Sam)

```text
오늘부터 재무 회계팀을 개소한다. 너는 개발 담당으로 파견이야.
이 팀의 원칙 세 가지를 먼저 기억해줘.
1) 모든 거래는 모의투자 연습 계좌에서만 한다
2) 주문은 어떤 경우에도 내 승인 없이 실행하지 않는다
3) 앱키와 계좌 정보는 화면에 출력하지 않는다
확인했으면 원칙 세 가지를 한 줄씩 요약해서 답해줘.
```

원칙을 먼저 문장으로 박아두는 것이 이 섹션의 안전장치입니다.
본인 업무에 쓸 때는 "돈·삭제·발송처럼 되돌리기 어려운 일"에 같은 패턴을 쓰세요.

## 2. 한도를 먼저 정한다 (사람이 직접)

에이전트에게 보내는 메시지가 아닙니다. `guardrails/limits.md` 를 **여러분이 직접** 작성하세요.

```markdown
# 투자 한도 (사람이 정하고, 에이전트는 읽기만 한다)

- 시드머니: 300만원 (모의)
- 1회 주문 한도: 1주
- 1일 주문 횟수: 1회
- 손실 한도: -10% 도달 시 전체 중단
- 매수 금지: 장 미개장 · 예산 소진 · 보유 상한 도달
- 오류 시: 재시도하지 않는다. 보고하고 멈춘다
```

연결보다 먼저 하는 이유가 있습니다. 연결부터 하고 안전장치를 나중에 붙이면
그건 옵션처럼 보입니다. **자기가 지킬 규칙을 자기가 쓰면 그건 규칙이 아닙니다.**

## 3. 연동과 첫 조회 (Sam)

```text
작업 폴더에 키움 모의투자 앱키를 넣어놨어. kiwoom-broker 스킬을 써서

1) 접근토큰을 발급받고 (토큰은 저장해서 재사용해. 요청마다 새로 받으면 안 돼)
2) KODEX 200(069500)의 종목 정보를 조회하고
3) 모의투자 계좌 잔고를 조회해서 보여줘.

앱키·시크릿·계좌번호 값은 출력하지 마. 주문은 오늘 하지 않는다.
```

마지막 두 문장이 핵심입니다. **비노출**과 **범위 제한**(오늘은 조회까지)을
요청 문장에 함께 넣는 패턴은 어떤 API 연동에든 그대로 옮겨 쓸 수 있습니다.

> 💡 잔고에 "모의투자 해당조회내역이 없습니다"가 나오면 **정상**입니다.
> 오류가 아니라 아직 아무것도 사지 않았다는 뜻입니다.

## 4. 키를 한 프로필에만 (개념 — 8.4에서 실제로 씀)

같은 저장소에서 여러 에이전트가 일할 때, 키 파일을 **집행 담당에게만** 줍니다.

```bash
# 집행 담당(Sam)만 키를 갖는다
KIWOOM_AUTH_ENV=~/.hermes/profiles/sam/kiwoom-mock.env \
  ~/.claude/skills/kiwoom-broker/scripts/kiwoom_api.sh kt00018 /api/dostk/acnt '{"qry_tp":"1","dmst_stex_tp":"KRX"}'
```

판단 담당(Ada)에게는 그 파일이 없으므로 **호출 자체가 시작되지 않습니다.**
권한을 안 준 일은 실수로도 못 하게 되는 것, 이게 8.4에서 쓸 설계입니다.

---

# 투자 DB 설계와 수집 (8.2)

> 실습 확정 후 추가됩니다.

# 규칙을 데이터로 검증하기 (8.3)

> 실습 확정 후 추가됩니다.

# 결정론적 자동 집행 (8.4)

> 실습 확정 후 추가됩니다.

# 투자위원회 (8.5)

> 실습 확정 후 추가됩니다.
