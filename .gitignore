# 인증 정보 — 절대 커밋 금지
.env
.env.*
!.env.example
*.token
*token.json

# 파이썬
__pycache__/
*.pyc
.venv/

# 노드
node_modules/

# OS
.DS_Store

# 실행 기록 — 각자의 판단 파일이라 커밋하지 않는다
state/

# 수집 산출물 — 원천 시세와 Supabase 적재 SQL은 로컬에만 둔다
artifacts/
