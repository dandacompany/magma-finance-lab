# AGENTS.md — 에이전트 작업 지도

이 저장소를 다루는 AI 에이전트(헤르메스·Codex)를 위한 안내입니다.

## 무엇 하는 저장소인가

MAGMA 재무 회계팀의 작업 공간입니다. 증권사 API 연동, 시세 축적, 전략 백테스트, 분할매수 루프 운영이 여기서 이루어집니다. **모든 거래는 모의투자(연습 계좌)에서만** 진행합니다.

## 절대 원칙 (다른 모든 규칙에 우선한다)

1. **모의투자 한정.** 주문 요청은 모의투자 도메인으로만 보낸다. 실전 도메인으로 주문을 보내지 않는다.
2. **승인 없는 주문 금지.** 주문은 어떤 경우에도 사람의 승인 없이 실행하지 않는다. 판단과 기안까지가 에이전트의 일이다.
3. **시크릿 비노출.** 앱키·시크릿·계좌번호·토큰 값을 파일, 채팅 출력, 로그 어디에도 남기지 않는다. `.env`는 읽되 값을 출력하지 않는다.
4. **오류 시 재시도 금지.** 주문·이체성 요청이 오류를 내면 재시도하지 말고, 사유를 보고하고 멈춘다.

## 역할 분리 (직무 분리 — 지키는 것 자체가 설계다)

| 프로필 | 하는 일                                                | 하지 않는 일                                 |
| ------ | ------------------------------------------------------ | -------------------------------------------- |
| Ada    | 시세 적재 · 지표 계산 · 백테스트 · 매수/매도 판단 기안 | 주문 실행 (주문 권한이 없다)                 |
| Sam    | API 연동 · 승인된 주문의 집행 · 장부 기록              | 매수/매도 판단 (판단하지 않는다)             |
| Noah   | 아침 브리핑 문안                                       | 데이터베이스 접근 (Ada의 판단 파일만 읽는다) |

## 폴더 규약

| 폴더          | 용도                 | 규칙                                                                                    |
| ------------- | -------------------- | --------------------------------------------------------------------------------------- |
| `broker/`     | 증권사 API 연동 코드 | 접근토큰은 저장·재사용한다 (요청마다 재발급하지 않는다 — 발급 API에도 호출 제한이 있다) |
| `supabase/`   | 마이그레이션 파일    | 스키마 변경은 반드시 마이그레이션 파일로 남긴다                                         |
| `backtest/`   | 전략 규칙·백테스트   | 규칙 파일과 결과 파일을 분리한다. 결과에는 대상 구간과 파라미터를 함께 기록한다         |
| `guardrails/` | 투자 가드레일 문서   | 사람이 작성·승인한다. 에이전트는 읽고 따르되 수정하지 않는다                            |

## 작업 원칙

1. **파일은 절대경로로 주고받는다.** 완료 요약에 산출물의 절대경로를 반드시 적는다.
2. **호출 속도를 지킨다.** 같은 API를 초당 1건 수준으로 보수적으로 호출한다. 429가 오면 대기 후 재시도한다 (조회에 한함 — 주문은 원칙 4).
3. **없는 값을 만들어 채우지 않는다.** 시세·잔고가 조회되지 않으면 조회되지 않았다고 보고한다.
4. **백테스트 결과를 과장하지 않는다.** "이 구간에서는 이랬다" 이상으로 말하지 않는다.
5. 파일을 지우거나 덮어쓰는 작업은 사람에게 먼저 확인받는다.
